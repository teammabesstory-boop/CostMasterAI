<Page
    x:Class="CostMasterAI.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Executive Dashboard 📊" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,24"/>

        <Grid Grid.Row="1" ColumnSpacing="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" Padding="20" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Total Resep" Opacity="0.6"/>
                    <TextBlock Text="{Binding TotalRecipes}" FontSize="48" FontWeight="Bold" Foreground="CornflowerBlue"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Column="1" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" Padding="20" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Total Bahan Baku" Opacity="0.6"/>
                    <TextBlock Text="{Binding TotalIngredients}" FontSize="48" FontWeight="Bold" Foreground="Orange"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Column="2" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" Padding="20" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Rata-rata Margin" Opacity="0.6"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding AverageMargin}" FontSize="48" FontWeight="Bold" Foreground="Green"/>
                        <TextBlock Text="%" FontSize="24" VerticalAlignment="Bottom" Margin="4,0,0,8" Foreground="Green"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>

        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.3">
            <TextBlock Text="CostMaster AI System" FontSize="32" HorizontalAlignment="Center"/>
            <TextBlock Text="Ready to optimize your profit." HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>